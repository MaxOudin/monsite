<nav class="fixed top-0 z-50 w-full backdrop-blur-sm bg-white/90 border-b border-gray-200 h-[52px] md:h-16">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-[52px] md:h-16">
      <!-- Logo -->
      <%= link_to root_path, class: "flex-shrink-0 flex items-center" do %>
        <%= image_tag "yellow_logo.svg", class: "w-10 h-10 rounded-full", alt: "Logo Maxime Oudin - Développeur Ruby on Rails Bordeaux" %>
        <span class="text-2xl text-gray-800 font-semibold ml-2">Maxime Oudin</span>
      <% end %>

      <!-- Actions visibles sur mobile -->
      <div class="flex md:hidden items-center space-x-2">
        <a href="#contact-infos" class="px-3 py-2 rounded-lg text-sm font-semibold text-gray-900 bg-white border border-gray-300 hover:bg-gray-50 transition-colors">
          Contact
        </a>
        <% if user_signed_in? %>
          <%= button_to 'Déconnexion', destroy_user_session_path, method: :delete, 
              class: "px-3 py-2 rounded-lg text-sm font-semibold text-gray-900 bg-white border border-gray-300 hover:bg-gray-50 transition-colors",
              data: { confirm: 'Êtes-vous sûr de vouloir vous déconnecter ?' } %>
        <% end %>
      </div>

      <!-- Menu principal -->
      <div class="hidden md:flex md:items-center md:space-x-8">
        <%= link_to "Projets", projets_path, 
            class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200 #{current_page?(projets_path) ? 'text-gray-900 font-semibold' : ''}" %>
        <%= link_to "Articles", articles_path, 
            class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200 #{current_page?(articles_path) ? 'text-gray-900 font-semibold' : ''}" %>

        <!-- CTA Desktop -->
        <div class="ml-4 flex items-center space-x-3">
          <a href="#contact-infos" class="inline-flex items-center justify-center px-4 py-2 rounded-lg text-sm font-semibold text-white bg-gray-900 hover:bg-gray-800 transition-colors">
            Contact
          </a>
          <% if user_signed_in? %>
            <%= button_to 'Déconnexion', destroy_user_session_path, method: :delete,
                class: "inline-flex items-center justify-center px-4 py-2 rounded-lg text-sm font-semibold text-gray-700 bg-white border border-gray-300 hover:bg-gray-50 transition-colors",
                data: { confirm: 'Êtes-vous sûr de vouloir vous déconnecter ?' } %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>

<!-- Spacer pour compenser la navbar fixe -->
<div class="h-[52px] md:h-16"></div>
