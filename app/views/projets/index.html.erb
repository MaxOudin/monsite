
<div class="cards-container" style="background:white; color: #141414; display: flex; flex-direction: column; align-items: center; justify-content: center; padding-top: 2vh;">
  <div style="width: 70%; display: flex; flex-direction: column; align-items: center; text-align: center; margin-top:2vh; margin-bottom:1vh;">
    <h3 style="font-weight: bold; text-align: left; margin-top: 2vh; margin-bottom: 2vh;">Mes derniers projets</h3>
    <p style="text-align: left;">Vous trouverez les précédentes réalisations significatives.
      Je m'adapte à vos besoins pour réaliser, ensemble, le site qui vous ressemble.
      Pour chacun des projets, vous avez la possibilité de voir les outils utilisés et un texte descriptif de celui-ci.</p>
  </div>
  <% if @projets.last != nil %>
    <% @projets.each do |projet| %>
      <div class="card" style="width: 24rem; margin-top:2vh; margin-bottom:2vh; margin-left: 2vw; margin-right: 2vw;
        border: 1px solid <%= projet.couleur %>;
        box-shadow: 0px 2px 6px rgba(0, 0, 0, 0.2);">
        <img class="card-img-top" src=<%= projet.image_url%> alt=<%= projet.image_url_alt %>>
        <div class="card-body">
          <h4 style="font-size: 16px;"><%= projet.type_projet.upcase %></h4>
          <h3 class="card-title" style="font-size: 28px;"><%= projet.titre %></h3>
          <p class="card-text" style="font-size: 16px;"><%= truncate(projet.description, length: 200) %></p>
        </div>
        <div style="display:flex; justify-content: flex-end;">
          <a href="projets/<%=projet.id %>" class="btn btn-primary"
              style="color: black; font-weight: bold; margin-right: 1vh; margin-bottom: 1vh;
              padding:1vh; padding-left: 2vw; padding-right: 2vw;
              border:<%= projet.couleur %>; background:<%= projet.couleur %>;">
              En savoir plus</a>
        </div>
      </div>
    <% end %>
    <% else %>
      <p>Aucun projet n'a été retrouvé</p>
  <% end %>

    <div style="display: flex; justify-content: flex-end; margin-top: 5vh; margin-bottom: 3vh;">
      <a href="/" class="btn btn-primary" style="background: #141414; color: white; border: 0px; font-weight: bold; font-size: 18px;
              display: flex;
              justify-content: flex-end;
              align-items: center;
              padding:1vh; padding-left: 2vw; padding-right: 2vw;">
              Retour à l'accueil</a>
    </div>

</div>
